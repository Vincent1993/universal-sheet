import { useState } from 'react'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <div className="App">
      <header className="App-header">
        <h1>Universal Sheet</h1>
        <p>A monorepo React project with modern tooling</p>
        <div className="card">
          <button onClick={() => setCount((count) => count + 1)}>
            count is {count}
          </button>
          <p>
            Edit <code>src/App.tsx</code> and save to test HMR
          </p>
        </div>
        <p className="read-the-docs">
          Built with React + TypeScript + Vite + Turborepo + pnpm
        </p>
      </header>
    </div>
  )
}

export default App